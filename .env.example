# --- Bajaj DMS credentials (REQUIRED) ---
# Your agency / employee ADID (will be uppercased automatically)
BAJAJ_USERNAME=A14796069
# The matching password
BAJAJ_PASSWORD=changeme

# --- Django ---
# Generate one with: python -c "import secrets; print(secrets.token_urlsafe(50))"
DJANGO_SECRET_KEY=replace-me-with-a-long-random-string
DJANGO_DEBUG=False
# Comma-separated list of hostnames the server is reachable as
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
# Comma-separated list of full origins for CSRF (https://your.domain)
DJANGO_CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000
